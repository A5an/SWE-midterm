{
  "name": "swe-midterm",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "docs:build": "node scripts/generate-complete-documentation.mjs",
    "dev:api": "python3 -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port ${PORT:-4000}",
    "dev:web": "[ -d apps/web ] && npm --workspace @swe-midterm/web run dev || echo \"apps/web is not present on this branch\"",
    "test": "python3 -m pytest backend/tests",
    "typecheck": "npm --workspace @swe-midterm/contracts run typecheck && npm --workspace @swe-midterm/api run typecheck && ([ -d apps/web ] && npm --workspace @swe-midterm/web run typecheck || true)",
    "build": "npm --workspace @swe-midterm/contracts run build && ([ -d apps/web ] && npm --workspace @swe-midterm/web run build || true)"
  },
  "devDependencies": {
    "markdown-it": "^14.1.0"
  }
}
