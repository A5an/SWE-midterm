# Video Evidence

Required manual artifact: two-browser-window recording showing:

1. Both windows load the same `documentId`.
2. Presence shows both users online after they join the collaboration session.
3. One window disconnects, continues editing locally, then reconnects.
4. The reconnecting window resyncs once and the other window reflects the final text.

Auth note:
- The trusted API-identity and access-check path added after review is covered by `docs/evidence/LIN-TEA-16/ws-test-log.txt`.
- The uploaded manual video remains the two-window collaboration artifact for presence and reconnect behavior.

CLI limitation:
- I cannot record or attach the actual two-window video from this terminal session.
- Recording uploaded and reviewer-accessible here:
  `https://www.youtube.com/watch?v=D5-8w6YMv_M`
