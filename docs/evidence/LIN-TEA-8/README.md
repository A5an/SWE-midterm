# LIN-TEA-8 Evidence Bundle

- Linear issue: `LIN-TEA-8`
- Branch used in this workspace: `FastAPI-skeleton/LIN-TEA-8`
- Base commit at start of local changes: `f537ed5b326da945ebadabc4db31512c55b86b42`
- PR link note: PR not opened from this workspace yet. Add the final PR URL here when the branch is pushed and the PR is created.

## Artifacts

- `startup.log`: FastAPI server startup and successful request log.
- `curl-create-load.txt`: `POST /documents` and `GET /documents/{id}` transcript.
- `pytest.txt`: backend pytest run for the migrated document endpoints.
